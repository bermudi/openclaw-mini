export default function RuntimeHomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem', maxWidth: '48rem' }}>
      <h1>OpenClaw Runtime</h1>
      <p>The runtime is running. Use the standalone dashboard package for operator UI.</p>
    </main>
  );
}
